# Next-Gen-voting-system-using-RFID
The **Next Generation Voting System using RFID** is an embedded system that uses an RFID card to authenticate voters and ensure secure voting. It is controlled by the **LPC2129 ARM7 Microcontroller**, and the voting status is displayed on a 16×2 LCD while preventing duplicate votes.
